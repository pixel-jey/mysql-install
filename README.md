# mysql-install
sudo /opt/sh/myql-config-install.sh install

shell once exec and so easy

# config
sudo \cp -r opt/* /

sudo sudo chown -R root:root /opt/sh/

sudo /opt/sh/myql-config-install.sh

Use: install|rootpwd|mycnf

# profile
export MYSQL_HOME=/usr/local/mysql/

export PATH=$MYSQL_HOME/bin:$PATH

# rootpwd
set root passwd

# mycnf
lost the mysql conf and reset default conf 	 

# version
mysql-8.0.31-linux-glibc2.12-x86_64.tar.xz
